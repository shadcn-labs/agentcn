---
title: Browser
description: Drives a real browser with Playwright using a snapshot-and-selector pattern to complete web tasks.
---

<DocsBaseSwitcher base="eve" agent="browser-agent" />

<AgentPreview
  agent="browser-agent"
  framework="eve"
  inputFields={[
    {
      name: "task",
      label: "Task",
      placeholder:
        "Go to news.ycombinator.com and list the top 3 story titles.",
      type: "textarea",
    },
  ]}
/>

## Summary

The **Browser Agent** completes tasks on the live web by driving a real Chromium
browser. It uses a snapshot-and-selector loop — the model sees a stable list of
interactive elements instead of brittle CSS — to navigate, click, and type. Reach
for it to automate flows, scrape interactive pages, or test UIs.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/eve/browser-agent
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install eve playwright
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/eve/browser-agent/agent/agent.ts"
  title="agent/agent.ts"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/instructions.md"
  title="agent/instructions.md"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/lib/browser.ts"
  title="agent/lib/browser.ts"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/tools/browser_goto.ts"
  title="agent/tools/browser_goto.ts"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/tools/browser_snapshot.ts"
  title="agent/tools/browser_snapshot.ts"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/tools/browser_click.ts"
  title="agent/tools/browser_click.ts"
/>

<ComponentSource
  src="registry/eve/browser-agent/agent/tools/browser_type.ts"
  title="agent/tools/browser_type.ts"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
agent/
├── agent.ts                  # Agent definition (model + config)
├── instructions.md           # Snapshot-then-act loop instructions
├── lib/
│   └── browser.ts            # Shared Playwright page + snapshot helper
└── tools/
    ├── browser_goto.ts       # Navigate to a URL
    ├── browser_snapshot.ts   # Read the current page + elements
    ├── browser_click.ts      # Click an element by selector
    └── browser_type.ts       # Type into an input by selector
```

## Customization

- **Managed remote browser.** `lib/browser.ts` connects to the [Bright Data
  Browser API](https://docs.brightdata.com/scraping-automation/scraping-browser) —
  a managed, anti-bot-resistant Chromium with no local infra. Set
  `BRIGHTDATA_BROWSER_AUTH` to your zone `username:password`, or point
  `BROWSER_CDP_URL` at any other CDP endpoint. With neither set it falls back to
  local Chromium for development.
- **Richer snapshots.** Extend `lib/browser.ts` to include roles, ARIA, or
  bounding boxes.
- **Add actions.** Add tools for scrolling, file uploads, or screenshots.
- **Swap the model.** Edit `agent/agent.ts`.
