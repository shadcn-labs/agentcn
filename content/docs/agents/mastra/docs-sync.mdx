---
title: Docs Sync
description: Keeps documentation synchronized with codebase changes.
---

<DocsBaseSwitcher base="mastra" agent="docs-sync" />

<AgentPreview
  agent="docs-sync"
  framework="mastra"
  inputFields={[
    {
      name: "input",
      label: "Sync Request",
      placeholder: "Sync docs with recent API changes",
      type: "text",
    },
  ]}
/>

## Summary

The **Docs Sync Agent** detects documentation drift by comparing docs against codebase changes. It identifies outdated examples, missing API references, and broken links, then generates update patches to keep documentation accurate.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/docs-sync
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/docs-sync/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/docs-sync/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
