---
title: Analytics Digest
description: Summarizes analytics data into actionable insights and trends.
---

<DocsBaseSwitcher base="mastra" agent="analytics-digest" />

<AgentPreview
  agent="analytics-digest"
  framework="mastra"
  inputFields={[
    {
      name: "input",
      label: "Analytics Query",
      placeholder: "What were last week's top pages?",
      type: "text",
    },
  ]}
/>

## Summary

The **Analytics Digest Agent** processes analytics data and generates concise summaries highlighting key metrics, trends, and anomalies. It helps stakeholders quickly understand performance without digging through dashboards.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/analytics-digest
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/analytics-digest/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/analytics-digest/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
