---
title: SEO Improver
description: Generates SEO improvements for pages including meta tags and content structure.
---

<DocsBaseSwitcher base="mastra" agent="seo-improver" />

<AgentPreview
  agent="seo-improver"
  framework="mastra"
  inputFields={[
    {
      name: "url",
      label: "Page URL",
      placeholder: "https://example.com/blog/post",
      type: "text",
    },
  ]}
/>

## Summary

The **SEO Improver Agent** analyzes page content and generates optimized meta titles, descriptions, heading structures, and internal linking suggestions. It provides concrete code snippets ready to implement.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/seo-improver
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/seo-improver/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/seo-improver/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
