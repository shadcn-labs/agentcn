---
title: Community Support
description: Handles Discord community support queries and ticket routing.
---

<DocsBaseSwitcher base="mastra" agent="community-support" />

<AgentPreview
  agent="community-support"
  framework="mastra"
  inputFields={[
    {
      name: "message",
      label: "Support Message",
      placeholder: "How do I reset my password?",
      type: "text",
    },
  ]}
/>

## Summary

The **Community Support Agent** triages Discord support messages, answers common questions from a knowledge base, and escalates complex issues to human moderators. It tracks conversation context to provide coherent multi-turn support.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/community-support
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/community-support/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/community-support/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
