---
title: Error Copy
description: Rewrites technical error messages into user-friendly copy.
---

<DocsBaseSwitcher base="mastra" agent="error-copy" />

<AgentPreview
  agent="error-copy"
  framework="mastra"
  inputFields={[
    {
      name: "error",
      label: "Error Message",
      placeholder: "ECONNREFUSED: Connection refused at port 5432",
      type: "text",
    },
  ]}
/>

## Summary

The **Error Copy Agent** transforms cryptic technical error messages into clear, helpful user-facing text. It preserves the technical context for developers while providing actionable guidance for end users.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/error-copy
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/error-copy/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/error-copy/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
