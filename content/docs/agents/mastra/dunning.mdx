---
title: Dunning Triage
description: Handles failed payment notifications and recovery workflows.
---

<DocsBaseSwitcher base="mastra" agent="dunning" />

<AgentPreview
  agent="dunning"
  framework="mastra"
  inputFields={[
    {
      name: "input",
      label: "Payment Event",
      placeholder: "Customer payment failed for subscription abc123",
      type: "text",
    },
  ]}
/>

## Summary

The **Dunning Triage Agent** manages failed payment recovery by categorizing failure reasons, drafting personalized customer communications, and recommending next actions based on customer history and failure patterns.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/dunning
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/dunning/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/dunning/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
