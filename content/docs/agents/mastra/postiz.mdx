---
title: Social Media Content
description: Creates and schedules social media posts across platforms.
---

<DocsBaseSwitcher base="mastra" agent="postiz" />

<AgentPreview
  agent="postiz"
  framework="mastra"
  inputFields={[
    {
      name: "topic",
      label: "Post Topic",
      placeholder: "Announce our new feature launch",
      type: "text",
    },
  ]}
/>

## Summary

The **Social Media Content Agent** drafts platform-optimized social posts, adapting tone and format for Twitter/X, LinkedIn, Instagram, and other channels. It suggests hashtags, posting times, and content variations for A/B testing.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/postiz
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/postiz/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/postiz/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
