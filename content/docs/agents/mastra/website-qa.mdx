---
title: Website QA
description: Automated QA testing for website functionality and visual regressions.
---

<DocsBaseSwitcher base="mastra" agent="website-qa" />

<AgentPreview
  agent="website-qa"
  framework="mastra"
  inputFields={[
    {
      name: "url",
      label: "Page URL",
      placeholder: "https://example.com",
      type: "text",
    },
  ]}
/>

## Summary

The **Website QA Agent** performs automated quality assurance checks including link validation, form testing, responsive design verification, and visual regression detection. It generates comprehensive QA reports with reproduction steps for any issues found.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/website-qa
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/website-qa/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/website-qa/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
