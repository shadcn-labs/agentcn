---
title: Standup Bot
description: Automates daily standup summaries from team updates.
---

<DocsBaseSwitcher base="mastra" agent="standup-bot" />

<AgentPreview
  agent="standup-bot"
  framework="mastra"
  inputFields={[
    {
      name: "updates",
      label: "Team Updates",
      placeholder: "List your yesterday, today, and blockers",
      type: "text",
    },
  ]}
/>

## Summary

The **Standup Bot Agent** aggregates individual team updates into structured standup summaries. It highlights blockers, tracks progress on sprint goals, and identifies items needing follow-up.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/mastra/standup-bot
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @mastra/core @mastra/memory
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/mastra/standup-bot/config.ts"
  title="config.ts"
/>

<ComponentSource
  src="registry/mastra/standup-bot/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── config.ts             # Agent configuration (model + memory)
├── instructions.md       # Agent instructions
└── memory.ts             # Memory configuration
```

## Customization

- **Swap the model.** Edit `config.ts`.
- **Modify instructions.** Edit `instructions.md`.
- **Adjust memory.** Edit `memory.ts`.
