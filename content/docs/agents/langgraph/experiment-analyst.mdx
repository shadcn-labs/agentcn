---
title: Experiment Analyst
description: Reads PostHog A/B experiment results and summarizes learnings.
---

<DocsBaseSwitcher base="langgraph" agent="experiment-analyst" />

<AgentPreview
  agent="experiment-analyst"
  framework="langgraph"
  inputFields={[
    {
      name: "input",
      label: "Input",
      placeholder: "Enter input...",
      type: "text",
    },
  ]}
/>

## Summary

The **Experiment Analyst** reads PostHog A/B experiment results and summarizes learnings. It uses the PostHog CLI to interact with experiment data, providing clear insights into what worked, what didn't, and recommendations for future experiments.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/langgraph/experiment-analyst
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @dawn-ai/sdk
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/langgraph/experiment-analyst/index.ts"
  title="index.ts"
/>

<ComponentSource
  src="registry/langgraph/experiment-analyst/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── index.ts              # Agent definition (model + config)
├── instructions.md       # Agent instructions
└── tools/                # Agent tools (if any)
```

## Customization

- **Swap the model.** Edit `index.ts`.
- **Modify instructions.** Edit `instructions.md`.
