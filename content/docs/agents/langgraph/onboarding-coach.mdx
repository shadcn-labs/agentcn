---
title: Onboarding Coach
description: Finds users stuck before activation and drafts nudge messages.
---

<DocsBaseSwitcher base="langgraph" agent="onboarding-coach" />

<AgentPreview
  agent="onboarding-coach"
  framework="langgraph"
  inputFields={[
    {
      name: "input",
      label: "Input",
      placeholder: "Enter input...",
      type: "text",
    },
  ]}
/>

## Summary

The **Onboarding Coach** identifies users who are stuck before completing activation and drafts personalized nudge messages to guide them forward. It uses the PostHog CLI to query user activation data and identify drop-off points in the onboarding flow.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/langgraph/onboarding-coach
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @dawn-ai/sdk
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/langgraph/onboarding-coach/index.ts"
  title="index.ts"
/>

<ComponentSource
  src="registry/langgraph/onboarding-coach/instructions.md"
  title="instructions.md"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── index.ts              # Agent definition (model + config)
├── instructions.md       # Agent instructions
└── tools/                # Agent tools (if any)
```

## Customization

- **Swap the model.** Edit `index.ts`.
- **Modify instructions.** Edit `instructions.md`.
