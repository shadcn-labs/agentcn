---
title: Analytics Digest
description: Pulls key event trends from PostHog and compiles a concise weekly digest with actionable insights.
---

<DocsBaseSwitcher base="flue" agent="analytics-digest" />

<AgentPreview
  agent="analytics-digest"
  framework="flue"
  inputFields={[
    {
      name: "input",
      label: "Input",
      placeholder: "Generate this week's analytics digest",
      type: "text",
    },
  ]}
/>

## Summary

The **Analytics Digest** pulls event trends from PostHog via the posthog-cli,
identifies significant changes in user behavior, highlights top-performing
features, flags anomalies, and compiles everything into a concise weekly digest
with actionable insights.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/flue/analytics-digest
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @flue/runtime
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/flue/analytics-digest/agents/analytics-digest.ts"
  title="agents/analytics-digest.ts"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
agents/
└── analytics-digest.ts    # Agent definition (model + config)
```

## Customization

- **Swap the model.** Edit `agents/analytics-digest.ts`.
