---
title: Google Sheets Analysis Agent
description: Flue recipe — read, analyze, and edit Google Sheets via the Sheets API.
---

The **Google Sheets Analysis Agent** reads a spreadsheet, answers questions about
it, and — on request — writes results back. It loads ranges in A1 notation,
computes totals/trends/joins, and updates cells only when you ask. Reach for it to
turn a sheet into a conversational analyst or to automate routine spreadsheet
edits.

<DocsBaseSwitcher base="flue" agent="google-sheets" />

The **Flue** recipe wires `read_range` and `update_range` into one agent. It reads
before writing and edits only the exact range you approve.

## Install

```bash
npx shadcn@latest add https://agentcn.vercel.app/r/flue/google-sheets
```

## Prerequisites

- A [Flue](https://flueframework.com) project (`npx flue@latest init my-agent`).
- The `@flue/runtime` package (installed automatically as a dependency of the recipe).
- A `GOOGLE_ACCESS_TOKEN` with the Sheets scope (OAuth user token or a service
  account token). Reading needs `spreadsheets.readonly`; writing needs
  `spreadsheets`.
- For the **live preview below**, set `ANTHROPIC_API_KEY`. The Flue preview runs
  in-process in this site's API route — no separate deployment required.

<AgentPreview
  agent="google-sheets"
  framework="flue"
  inputFields={[
    {
      name: "spreadsheetId",
      label: "Spreadsheet ID",
      placeholder: "1A2b3C4d5E6f7G8h9I0j…",
      type: "text",
    },
    {
      name: "request",
      label: "Request",
      placeholder: "Total revenue by region and write it to F1.",
      type: "text",
    },
  ]}
/>

## Files

<Accordion type="single" collapsible>
  <AccordionItem value="files">
    <AccordionTrigger>What gets installed</AccordionTrigger>
    <AccordionContent>

```txt
agents/
└── google-sheets.ts     # Agent with read + update tools
tools/
├── read-range.ts        # Reads a range (A1 notation)
└── update-range.ts      # Writes values to a range
```

    </AccordionContent>

  </AccordionItem>
</Accordion>

## Customization

- **Use a service account.** Mint the access token from a service account for
  unattended runs.
- **Add operations.** Add tools for appending rows, formatting, or creating sheets.
- **Guard writes.** Require explicit confirmation before any `update_range` call.
- **Swap the model.** Change the `model` field on the agent.
